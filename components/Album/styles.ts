import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: { width: 160, margin: 10 },
  image: { width: "90%", height: 160 },
  text: { color: "gray", marginTop: 10, textAlign: "center" },
});

export default styles;
